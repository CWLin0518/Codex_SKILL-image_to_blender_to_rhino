# 商品圖 → Blender 建模 → Luna 轉檔 → Rhino 3DM：SOP Prompt

使用方式：複製下方「可直接貼上的 Prompt」全部內容，填入輸入路徑、輸出路徑與模型名稱，再交給具有 Blender、Rhino MCP 與子代理工具的代理執行。

## 可直接貼上的 Prompt

請用 orchestrator 統籌流程，讀取指定資料夾內的商品圖與尺寸圖，在 Blender 建立模型，再交由 **Luna（gpt-5.6-luna）** 負責匯出、匯入 Rhino，最後儲存可開啟的 `.3dm`。主代理負責需求判讀、建模、視覺檢查、整合與最終獨立驗證，請實際完成檔案，不要只提供操作建議。

### 1. 任務設定

- 商品圖資料夾：`【當前資料夾路徑】`
- 輸出資料夾：`【當前資料夾路徑】`
- 模型名稱：`【填入名稱】`
- Blender 建模單位：公尺。
- Rhino 交付單位：毫米。
- 預設交付類型：保留部件與基本材質配色的網格模型。
- 精度依據：尺寸圖優先；照片補充形狀、比例、結構與細節。
- 預設未提供的尺寸：依照片合理推估，列入說明檔，不宣稱是原廠精確尺寸。

### 2. 授權範圍與協作方式

此任務明確授權主代理使用子代理分工，並指定 Luna 執行後段轉檔。正常、可逆且在任務範圍內的步驟請自行完成，不需逐步詢問確認。

若有可用的 orchestrator 技能，先讀取並遵循；若找不到，明確告知，改以主代理統籌和子代理委派實現流程，不要假稱已使用該技能。若無法呼叫 Luna，回報限制，不要將其他模型冒稱為 Luna。

建立模型期間，Luna 可先唯讀檢查 Rhino 連線與匯出 API。主代理發出 `READY` 後，才由 Luna 操作匯出與匯入。避免兩個代理同時修改同一個 Blender 場景或 Rhino 文件。

不要刪除使用者既有物件、清空場景、關閉未儲存文件或覆寫來源圖片。使用專用 collection／圖層管理本次模型；若輸出檔名已存在，使用版本後綴保留既有檔案。

### 3. 前置檢查

1. 列出指定路徑的檔案，實際查看全部相關商品圖、細節圖與尺寸截圖。
2. 檢查 Blender MCP 的 addon 狀態、版本及目前場景，不預設場景是空白。
3. 檢查 Rhino MCP 的可用 slots 與文件內容。優先使用適合本任務的既有實例；沒有時，嘗試建立新的實例。若連線仍不可用，回報具體阻礙，並完成不受阻礙的工作。
4. 查閱本次會用到的匯出／匯入 API 及 enum，不硬編碼未確認的版本相關選項。
5. 簡短告知判讀到的主要尺寸、需推估的部位及交付類型。

### 4. 圖片判讀與建模規劃

整理主體輪廓、部件數量、結構關係、材質與辨識特徵。以家具為例，檢查：底座、坐墊、靠背、扶手、支架、腳、接縫、滾邊、固定帶、鈕扣等。

尺寸圖中的寬、深、高、座高、座深、背寬要分別解讀。不要把座深當成總深；不確定的數值標示為推估。預先約定座標方向，例如 X 為寬、Y 為深、Z 向上，並記錄前方朝向。

### 5. 主代理：Blender 建模

1. 建立以模型名稱命名的專用 collection，各部件使用清楚且穩定的名稱。
2. 先建立符合主要尺寸的主體，再補輪廓、圓角、厚度、傾角與細節。
3. 軟墊、布面應具有合理弧度；滾邊應貼合表面，避免浮空、穿插或明顯重疊。
4. 建立基本材質與配色；布料可加程序紋理，但不能依賴程序紋理才能辨識模型。
5. 以節點類型查找 shader，不依賴可能受語系影響的節點顯示名稱。
6. 控制網格密度，兼顧外形與可操作性；保留有意義的部件分離。
7. 用視窗截圖檢查外觀，必要時查看正面、側面、背面或細節，並取得場景資訊確認物件存在。
8. 修正明顯比例錯誤、接縫浮空、法線問題與缺件後，儲存 `.blend`。

若 Blender MCP safe mode 禁止 `open`、`exec`、`os` 等操作，改用允許的直接建模指令及原生儲存／匯出 operator。不要試圖繞過限制，也不要將關閉安全模式當成預設解法。

保留可重現的建模腳本；最終修正也要同步更新腳本。若腳本與 safe mode 的執行方式不同，寫明使用方式。

### 6. 交接 Luna

主代理完成 Blender 檢查後，送出以下交接資訊：

> READY：Blender 建模完成。輸出路徑：【路徑】；模型 collection：【名稱】；預期部件數：【數量】；Blender 單位：公尺；座標方向：【方向】。請只匯出本次 collection，包含曲線轉換後的可見幾何與 modifier 結果。完成 OBJ／MTL、Rhino 匯入與 3DM 儲存，保留名稱、部件與基本材質。不要修改造型或既有無關物件。完成後提供檔案路徑、部件數、實測尺寸、網格有效性及存檔讀回結果；遇到阻礙請即時回報。

### 7. Luna：匯出 Blender 模型

1. 使用 Blender 原生匯出 operator，產出 OBJ 與 MTL。
2. 限定匯出指定 collection／選取物件，排除預設立方體、相機、燈光與無關物件。
3. 包含曲線實體化的幾何、支架、滾邊及其他細節，確認 evaluated mesh／modifier 結果有被匯出。
4. 明確設定並記錄座標軸與比例，避免鏡射、倒置或重複縮放。
5. 檢查 OBJ 的實際部件結構。OBJ 可能以 `g` 而非 `o` 記錄部件；同一部件可能出現多次群組切換。若自行解析，須合併正確的部件資料，避免漏面、重複幾何或空群組。
6. 確認材質檔存在，並保留物件名稱及材質對應。

### 8. Luna：匯入 Rhino 並儲存

1. 明確指定 Rhino slot，腳本使用工具提供的 `__rhino_doc__`，不依賴可能指向其他文件的 ActiveDoc。
2. 將 OBJ 匯入 Rhino；若原生匯入流程不可靠，可解析 OBJ／MTL，建立 Rhino Mesh 與材質。
3. 若交換檔座標為公尺，轉換成毫米時乘以 1000，且僅轉換一次。檢查實際座標，不能只改文件單位標籤。
4. 保留部件名稱，使用本次模型的專用圖層，保留基本配色並將材質明確指定到物件或圖層。
5. 檢查 mesh validity、法線與部件數；檢查是否有遺漏、重複或尺寸異常。
6. 設定合適的透視角度、Shaded／Rendered 顯示及取景，確認模型可見。
7. 儲存 `.3dm`，只包含本次交付模型，避免混入使用者無關物件。

### 9. 主代理：獨立驗證最終 3DM

不要只採信子代理的完成訊息或畫面中看得到模型。主代理必須直接讀取實際儲存的 `.3dm`，核對：

- 檔案存在、大小合理、可以讀取。
- 單位為毫米。
- 部件數符合預期；若合理拆分或合併，說明原因。
- 所有網格幾何有效。
- Bounding box 的寬、深、高符合尺寸圖或已記錄的推估。
- 名稱、材質表、物件／圖層材質指派均已寫入檔案。
- 特徵配色正確，例如藍色鈕扣不應在轉檔後變白。

**材質驗證是必要步驟：** Rhino 工作階段存在材質，不代表 `.3dm` 已保存材質。若檔案讀回後材質遺失，修正序列化／材質索引與指派，再重新儲存和讀回。使用自建 File3dm 時，要明確加入圖層、材質與物件，並使用正確的檔案內材質索引。

讀回驗證不必另外開啟新的 Rhino 實例。若可用 File3dm 直接讀檔，就採用這種方式，不讓非必要的新實例啟動阻擋交付。

最後查看 Rhino 模型截圖，確認造型、方向與配色。若截圖工具將圖片包在文字 JSON 的巢狀 `content` 中，先解析再顯示圖片，不要輸出大量 base64。

### 10. 交付內容

在指定輸出資料夾留下：

- `【模型名稱】.3dm`：主要 Rhino 交付檔。
- `【模型名稱】.blend`：可繼續編輯的 Blender 原始檔。
- `【模型名稱】.obj` 與 `.mtl`：交換檔與基本材質。
- 建模腳本：可重現模型建立步驟。
- `MODEL_NOTES.md`：尺寸依據、推估項目、座標與單位、部件說明、轉檔限制及最終實測尺寸。

完成回覆保持簡潔，提供 `.3dm` 與 `.blend` 的可點擊完整路徑，列出有效物件數與尺寸，並說明：

> 本模型依商品照片與尺寸圖重建；未提供尺寸的細節採近似比例。Rhino 成品為網格模型，非原廠精確 NURBS／製造用 CAD。Blender 的程序布紋未必能完整移轉，Rhino 版本以幾何及基本配色為交付重點。

請持續執行直到所需檔案完成且驗證通過。只有必要步驟確實受到外部條件阻擋，才回報已完成成果、具體阻礙與需要的最小協助；不要把尚未驗證的檔案標示成已完成。